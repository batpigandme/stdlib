{{alias}}( pkg )
    Returns catalog metadata for a specified package name.

    Parameters
    ----------
    pkg: string
        Package name.

    Returns
    -------
    out: Object|null
        Package metadata.

    Examples
    --------
    > var v = {{alias}}( '@stdlib/math/base/special/sin' )
    {...}

    See Also
    --------
